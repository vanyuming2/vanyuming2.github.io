# 项目协作规则

本仓库是一个长期迭代的私人纪念网站，并包含“另一种人生”人生重开游戏、异闻、特殊事件与星空花园。开始任何工作前，先以代码和测试为准恢复上下文，不要只依赖聊天记录。

## 开始工作前

按顺序阅读：

1. `docs/CURRENT_STATE.md`：当前已经实现的内容、已知问题与近期目标。
2. `docs/GAME_BIBLE.md`：世界观、剧情层级、特殊事件和文案规则。
3. `docs/TECHNICAL_MAP.md`：代码、数据、存档、素材和部署结构。
4. `docs/DECISIONS.md`：已经确认的重要决定，避免无意回退。
5. 涉及图片时再读 `docs/ASSET_MANIFEST.md`。

随后检查 `git status` 和相关测试。若文档与代码冲突，以当前可运行代码和用户最新明确要求为准，并在本轮结束前修正文档。

## 权限与 Git

- 所有视觉与玩法修改先在本地展示。除非用户在当前请求中明确要求，否则不得 push、部署、发布或修改远端状态。
- “GitHub 已登录”不等于允许发布。
- 不使用 `git reset --hard`、`git clean`、强制 checkout 或 force push。
- 工作区可能包含用户素材和未提交成果；不得覆盖、移动或删除不属于本次任务的文件。
- 用户认可一个阶段后，优先创建本地检查点提交。提交前运行与改动相称的测试，并审查暂存内容。
- 不把密钥、Cookie、Token、浏览器登录信息或个人账号凭据写入代码、文档和提交。

## 产品与视觉原则

- 主页现在定位为万雨铭的个人小站；既有私人记忆可以继续保留，但新增公开叙事不再默认围绕两个人的关系。未经明确要求，不擅自删除旧内容。
- 主视觉基准是安静的深色夜空、微弱暖光和轻轻闪烁的小星星。温暖、克制、成熟、耐看优先于炫技。
- 不重新加入巨型姓名、巨型标题、学历工作介绍、规整人物履历或大段直白煽情文案。
- 新装饰应像从同一片夜空自然长出；避免持续强动画、过量光效、像素风和与现有平面语言冲突的元素。
- 花园当前是独立的基础 3D 观赏场景。长期方向是俯视摆放、收藏和存档，不做可操控小人。

## 游戏与叙事原则

- 原版普通人生继续使用 Life Restart 的简体中文数据和核心规则；本站怪谈、铺垫、结局在独立覆盖层中添加，不能破坏原数据引用。
- 怪谈语言保持克制。只呈现异常、动作和后果，不替玩家解释真相，不在结尾总结“这一切意味着什么”。“未加载的世界”入口参考句为：“你伸手掀开帷幕的一角。下一秒，周围的一切开始无声褪去。”
- 特殊事件先出现在逐年人生记录中，再显示进入提示。强制事件只有进入按钮；可选事件提供进入和跳过，并提示是否已经收录。
- `outcome: "resume"` 的故事读完后必须继续当前人生；只有明确标记 `end-life` 的特殊结局才结束本局。
- 需要男性主角的天赋或结局必须同时做到：0岁强制男性出生、过滤女性限定天赋、调试人生也遵守同一规则。不得在天赋说明里机械写“你生为男性”。
- 特殊事件的触发年龄、前置天赋、图片页数、图片顺序和结束方式必须数据化并有测试。
- 调试入口只在开发环境或本机地址出现；生产构建不得显示调试按钮或开放预览捷径。
- 结局层级保持清楚：读完任意特殊事件获得“初识冰山一角”；完成修仙线获得“完美结局”；下一轮选择红色药丸并走完“未加载的世界”后获得“真结局”。
- 真结局引用固定为：“我再一次站在阳光下，就像我儿时那样，我一无所有，一无所学，一无所知。”署名“赫尔曼·黑塞《悉达多》”。

## 素材与许可

- 发布素材位于 `public/`；原始抓取与映射位于 `素材库/`。先核对清单，再复制、裁剪或替换。
- 不绕过平台权限，不保存登录凭据。用户已说明相关图文已取得作者同意并用于非商业私人项目，但新增来源仍要记录链接、页数和处理方式。
- 不擅自删除、重排、重复压缩故事图片。去除平台界面时优先裁切，不修改叙事主体。
- Life Restart 的 MIT 完整声明必须继续随发布产物分发在 `public/third-party-notices.txt`。页面可以不展示显眼的许可文案，但许可文件不能删除。

## 验证要求

- 常规检查：`npm.cmd run lint`。
- 完整检查：`npm.cmd test`，其中包含静态构建和全部 Node 测试。
- 修改特殊事件时至少运行 `tests/special-endings.test.mjs`；修改异闻线时运行 `tests/weird-tales.test.mjs`；修改引擎时运行 `tests/remake-engine.test.mjs` 和 `tests/remake-data.test.mjs`。
- 新增特殊事件必须测试：资源存在、页数与首尾路径、触发年龄边界、前置天赋、性别限制、强制/可选入口、读完后继续或结束人生。
- GitHub Pages 发布产物固定为 `dist/client`，静态路由使用尾斜杠。

## 文档维护

- 每个被用户确认的阶段结束时，同步更新 `CURRENT_STATE.md`、`GAME_BIBLE.md`、`DECISIONS.md` 和必要的素材清单。
- `CURRENT_STATE.md` 只写现在；历史原因写入 `DECISIONS.md`，不要让状态文档无限增长。
- 关键规则尽量同时转成测试。文档是说明，测试是可执行的项目记忆。
- 创建检查点或交接时更新 `HANDOFF.md`，并记录本轮验证结果；不要复制整段聊天记录。
